var browserify = require('browserify')();

module.exports = {
  jxaCompile: function( sourceFile ) {
    // now wrap the file and run osacompile on it, passing osacompile args from the command line

  }

}

